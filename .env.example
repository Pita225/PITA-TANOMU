# Current Render Free / training defaults: both features are strict no-ops.
BACKUP_ENABLED=false
AUDIT_SNAPSHOT_ENABLED=false

# Used only after the corresponding feature is enabled.
BACKUP_BACKEND=sqlite
BACKUP_STORAGE_PATH=/var/data/pita/backups
AUDIT_STORAGE_PATH=/var/data/pita/audit
